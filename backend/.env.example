# Port server
PORT=3000
NODE_ENV=production

# Connection string Neon PostgreSQL (Neon Dashboard > Connection Details).
# Gunakan yang mengandung "-pooler" di hostname untuk koneksi yang lebih stabil.
DATABASE_URL=

# JWT Authentication Secret Key
JWT_SECRET=bapperida-rka-jwt-secret-2026-ganti-segera

# Konfigurasi AI API Keys (bisa diisi di sini atau via panel Admin UI)
GEMINI_API_KEY=
OPENAI_API_KEY=
